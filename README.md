# نظام العضوية والانتخابات — مأتم الرضا عليه السلام - المالكية

نظام متكامل لاستقبال طلبات العضوية بالجمعية العمومية وإدارتها، ولإجراء انتخابات مجلس الإدارة.
الواجهة بالكامل باللغة العربية ومن اليمين إلى اليسار، وتعمل على الهواتف والأجهزة اللوحية وأجهزة الحاسب.

---

## المحتويات

- [نظرة عامة](#نظرة-عامة)
- [التشغيل عبر Docker](#التشغيل-عبر-docker)
- [متغيرات الإعداد](#متغيرات-الإعداد)
- [الحسابان الإداريان](#الحسابان-الإداريان)
- [دليل الاستخدام](#دليل-الاستخدام)
- [النسخ الاحتياطي](#النسخ-الاحتياطي)
- [ملاحظات فنية](#ملاحظات-فنية)

---

## نظرة عامة

| العنصر | التفصيل |
|---|---|
| لغة الخادم | Go 1.22 |
| الواجهة | JavaScript حديث (ES modules) بدون أي إطار عمل |
| قاعدة البيانات | SQLite داخل مجلد البيانات |
| المسارات العامة | `/` تسجيل الدخول، `/register` الاستمارة، `/member` صفحة العضو، `/vote` التصويت |
| لوحة إدارة العضوية | العنوان السري المحدد في `ADMIN_PATH` |
| لوحة لجنة الانتخابات | العنوان السري المحدد في `ELECTIONS_PATH` |

اللوحتان منفصلتان تماماً: لكل منهما حساب مستقل وجلسة مستقلة، ولا تستطيع أي منهما الوصول إلى بيانات الأخرى أو وظائفها.

### ما تم استيراده

تم استيراد **378 عضواً** من الكشف الأصلي `اعضاء_الجمعية_العمومية.xlsx` عند أول تشغيل، مع معالجة ما يلي:

- **إعادة الأصفار المفقودة إلى الأرقام الشخصية.** كان Excel قد حذف الصفر الأول من 36 رقماً (مثال: `304450` أُعيد إلى `000304450`). جميع الأرقام الآن من تسعة أرقام.
- **تحويل تواريخ الميلاد** من الصيغة العربية (`نوفمبر ٦, ١٩٧١`) إلى تاريخ فعلي.
- **تحويل الأرقام العربية الهندية** (٠١٢٣٤٥٦٧٨٩) إلى أرقام لاتينية في كل الحقول الرقمية.

> **تنبيه:** في الكشف الأصلي توجد ثماني مجموعات من الأعضاء تشترك في رقم هاتف واحد (أفراد أسرة واحدة). تم الإبقاء عليهم كما هم، ويُمنع التكرار على الطلبات الجديدة فقط. الدخول يبقى دقيقاً لأن الرقم الشخصي فريد لكل عضو.

---

> **للنشر على خادم عبر Portainer و Cloudflare:** راجع ملف `DEPLOY.md` للدليل الكامل خطوة بخطوة.

## التشغيل عبر Docker

```bash
# 1) تجهيز كلمات المرور
cp .env.example .env
nano .env          # غيّر كلمتي المرور

# 2) التشغيل
docker compose up -d --build

# 3) متابعة السجل
docker compose logs -f
```

يستمع النظام على `127.0.0.1:8080`. ضع أمامه خادماً عكسياً لإنهاء TLS، مثال Nginx:

```nginx
server {
    listen 443 ssl http2;
    server_name membership.example.org;

    ssl_certificate     /etc/letsencrypt/live/membership.example.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/membership.example.org/privkey.pem;

    client_max_body_size 8m;   # صور المرشحين

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

> استخدام HTTPS ضروري. عند `COOKIE_SECURE=true` لا يُرسل المتصفح ملف الجلسة عبر اتصال غير مشفّر، فلن يعمل تسجيل الدخول على `http://` عدا `localhost`.

### التشغيل بدون Docker

```bash
go build -o matam .
DATA_DIR=./data LISTEN_ADDR=:8080 COOKIE_SECURE=false \
ADMIN_PASSWORD=... ELECTIONS_PASSWORD=... ./matam
```

---

## متغيرات الإعداد

| المتغير | الافتراضي | الوظيفة |
|---|---|---|
| `DATA_DIR` | `./data` | قاعدة البيانات والصور وملف التصدير |
| `LISTEN_ADDR` | `:8080` | عنوان الاستماع |
| `COOKIE_SECURE` | `false` | اجعله `true` في الإنتاج |
| `MAX_UPLOAD_MB` | `5` | الحد الأقصى لحجم صورة المرشح |
| `REGISTER_LIMIT_PER_HOUR` | `40` | عدد الطلبات المسموحة من عنوان شبكة واحد في الساعة |
| `ADMIN_USERNAME` / `ADMIN_PASSWORD` | `admin` / عشوائية | حساب إدارة العضوية، عند أول تشغيل فقط |
| `ELECTIONS_USERNAME` / `ELECTIONS_PASSWORD` | `elections` / عشوائية | حساب لجنة الانتخابات، عند أول تشغيل فقط |
| `ADMIN_PATH` | `admin` | العنوان السري للوحة إدارة العضوية |
| `ELECTIONS_PATH` | `elections` | العنوان السري للوحة لجنة الانتخابات |
| `TRUST_PROXY` | `false` | اجعله `true` خلف Cloudflare أو أي خادم عكسي |
| `TZ` | `Asia/Bahrain` | التوقيت المستخدم في مواعيد الإغلاق |

إذا لم تُحدَّد كلمة المرور عند أول تشغيل، يولّدها النظام عشوائياً ويطبعها في السجل مرة واحدة. احفظها فوراً.

---

## الحسابان الإداريان

### كيف تصل إلى اللوحتين

لا يوجد رابط ظاهر للوحتين في أي صفحة. الدخول يتم عبر علامة صغيرة في أسفل الصفحة على شكل معيّن نحاسي، تبدو جزءاً من الزخرفة:

- **على الهاتف:** اضغط عليها مع الاستمرار لثانية تقريباً.
- **على الحاسب:** انقر عليها خمس نقرات متتابعة.

تفتح عندها نافذة تطلب اسم المستخدم وكلمة المرور. يتعرف النظام تلقائياً على نوع الحساب وينقلك إلى لوحتك المناسبة، فلا حاجة لحفظ أي عنوان.

| اللوحة | اسم المستخدم | كلمة المرور |
|---|---|---|
| إدارة العضوية | `admin` | `Isa-Jidhafs-1491-$` |
| لجنة الانتخابات | `elections` | `Manama-Jidhafs-1197-$` |

عناوين اللوحتين لا تُكتب في أي صفحة ولا في أي ملف يصل إلى المتصفح، ولا تظهر إلا بعد نجاح تسجيل الدخول. من يجد العلامة لا يرى سوى نافذة تسجيل دخول.

يمكن أيضاً الدخول مباشرة عبر العنوان السري المحدد في `ADMIN_PATH` و `ELECTIONS_PATH` داخل ملف `.env`. القيمة الافتراضية إذا لم تُضبط هي `/admin` و `/elections`.

### استعادة الدخول عند نسيان كلمة المرور

لا توجد وسيلة لاستعادة كلمة المرور عبر المتصفح، لأنها مخزّنة مشفّرة ولا يمكن قراءتها. الاستعادة تتم من الخادم فقط:

```bash
# 1) ضع كلمة المرور الجديدة في .env وفعّل متغير الاستعادة
ADMIN_PASSWORD=كلمة-المرور-الجديدة
ADMIN_PASSWORD_RESET=true

# 2) أعد التشغيل مرة واحدة
docker compose up -d

# 3) تأكد من السجل ثم أعد المتغير إلى false وأعد التشغيل
docker compose logs --tail=20 matam
```

استخدم `ELECTIONS_PASSWORD_RESET` بنفس الطريقة لحساب لجنة الانتخابات. الاستعادة تؤثر على الحساب المحدد وحده، ولا تمس الأعضاء ولا الأصوات ولا الحساب الآخر.

> أعد المتغير إلى `false` بعد الاستعادة. تركه مفعّلاً يعيد ضبط كلمة المرور عند كل إعادة تشغيل.

## دليل الاستخدام

### لوحة إدارة العضوية — `/admin`

| التبويب | الوظيفة |
|---|---|
| **طلبات العضوية** | مراجعة الطلبات الواردة، اعتمادها أو رفضها أو حذفها. لا يُنشأ حساب للعضو إلا بعد الاعتماد. |
| **الأعضاء** | البحث في كشف الأعضاء، تعديل بيانات أي عضو، التحفّظ على عضويته أو إلغاؤها أو حذفه نهائياً. |
| **تحرير الاستمارة** | تعديل العنوان والشروط ونص الإقرار، تعديل مسميات الحقول، وإضافة حقول جديدة أو حذفها أو إعادة ترتيبها. |
| **نموذج الطباعة** | رفع ملف Word لاستمارة العضوية الورقية، ومراجعة الرموز المستخدمة فيه. |
| **التسجيل والتصدير** | فتح التسجيل أو إغلاقه فوراً أو تحديد موعد إغلاق مستقبلي، وتنزيل كشف الأعضاء. |

**الحقول المحمية:** الاسم والرقم الشخصي وتاريخ الميلاد ورقم التواصل لا يمكن حذفها ولا تعطيلها، لأنها بيانات الدخول نفسها. يمكن تغيير مسمياتها ونصوصها الإرشادية.

**موعد الإغلاق المجدول** يظهر تلقائياً في شريط أعلى صفحة التسجيل.

**التصدير:** أي اعتماد أو تعديل أو حذف يعيد بناء ملف `اعضاء_الجمعية_العمومية.xlsx` داخل مجلد البيانات مباشرة. الأرقام الشخصية مخزّنة بصيغة نصية، فلا يحذف Excel الصفر الأول لا عند الفتح ولا عند إعادة الحفظ.

### حالات العضوية

لكل عضو حالة يضبطها مسؤول العضوية من زر **تحفّظ / إلغاء** بجانب اسمه:

| الحالة | في الكشف و Excel | تسجيل الدخول | التصويت والخدمات |
|---|---|---|---|
| **عضوية نشطة** | نعم | مسموح | مسموح |
| **تم التحفّظ** | نعم، مع بيان الحالة | مسموح، ويرى: يرجى مراجعة المأتم | ممنوع |
| **عضوية ملغاة** | نعم، مع بيان الحالة | ممنوع، ويرى: يرجى مراجعة المأتم | ممنوع |
| **حذف نهائي** | يُزال تماماً | — | — |

**التحفّظ والإلغاء لا يحذفان العضو.** يبقى في قاعدة البيانات وفي ملف Excel مع عمود **حالة العضوية**، ويمكن إعادة تفعيله في أي وقت. الحذف النهائي وحده يزيل السجل، وهو زر منفصل.

عند التحفّظ أو الإلغاء تُغلق جلسات العضو المفتوحة فوراً، فلا ينتظر انتهاء الجلسة الحالية.

يمكن تسجيل **ملاحظة داخلية** مع كل تغيير. تظهر في عمود **ملاحظة الحالة** في ملف Excel للإدارة، ولا يراها العضو.

**عدد الناخبين** في لوحة لجنة الانتخابات يحسب الأعضاء النشطين فقط، فلا يضخّم المتحفّظ عليهم نسبة المشاركة.

### حالات الطلبات

الخيارات نفسها متاحة للطلبات التي لم تُعتمد بعد، من زر **تحفّظ / إلغاء** في بطاقة الطلب:

- **تم التحفّظ:** يرى مقدّم الطلب عند محاولة الدخول: يرجى مراجعة المأتم.
- **إلغاء الطلب:** الرسالة نفسها، والطلب مغلق.
- **قيد المراجعة:** إعادة الطلب إلى الوضع الطبيعي.

الطلب المعتمد لا يمكن التحفّظ عليه من هنا، لأن العضوية أصبحت قائمة؛ يتم ذلك من تبويب الأعضاء.

### نموذج الطباعة (ملف Word)

يمكن رفع ملف Word يمثل الاستمارة الورقية، فيملأ النظام بياناته تلقائياً ويهيئه للطباعة.

**كيف تعد الملف:** اكتب استمارتك في Word بالتنسيق الذي تريده تماماً، وضع في مواضع البيانات رموزاً بهذا الشكل:

```
الاسم الرباعي: {{name}}
الرقم الشخصي: {{cpr}}
تاريخ الميلاد: {{dob}}
العنوان: {{address}}

قرار اللجنة:  {{status_accepted}} مقبول    {{status_rejected}} غير مقبول
```

ثم ارفعه من تبويب **نموذج الطباعة**. يعرض النظام بعد الرفع كل الرموز المتاحة، ويميّز المستخدم منها في ملفك، وينبّهك إلى أي رمز كتبته خطأً.

**الطباعة:** يظهر زر **طباعة** بجانب كل عضو في تبويب الأعضاء، وزر **طباعة الاستمارة** مع كل طلب في تبويب الطلبات. ينزّل الملف باسم العضو جاهزاً للطباعة.

#### الرموز المتاحة

| الرمز | المحتوى |
|---|---|
| `{{name}}` | الاسم الرباعي |
| `{{cpr}}` | الرقم الشخصي بتسعة أرقام |
| `{{dob}}` | تاريخ الميلاد |
| `{{phone}}` | رقم التواصل |
| `{{email}}` | البريد الإلكتروني |
| `{{house}}` `{{road}}` `{{block}}` | المنزل والطريق والمجمع، كل على حدة |
| `{{address}}` | العنوان كاملاً في سطر واحد |
| `{{volunteer}}` `{{volunteer_field}}` | العمل التطوعي والمجال |
| `{{affiliated}}` `{{affiliation}}` | الانتساب لمؤسسة أخرى واسمها |
| `{{status}}` | مقبول / غير مقبول / قيد المراجعة |
| `{{status_accepted}}` | ☑ إذا كان مقبولاً، وإلا ☐ |
| `{{status_rejected}}` | ☑ إذا كان مرفوضاً، وإلا ☐ |
| `{{status_pending}}` | ☑ إذا كان قيد المراجعة، وإلا ☐ |
| `{{volunteer_yes}}` `{{volunteer_no}}` | مربعا الاختيار لسؤال العمل التطوعي |
| `{{affiliated_yes}}` `{{affiliated_no}}` | مربعا الاختيار لسؤال الانتساب لمؤسسة أخرى |
| `{{meeting_no}}` | رقم اجتماع مجلس الإدارة الذي عُرض فيه الطلب |
| `{{membership_no}}` | رقم العضوية الثابت |
| `{{secretary}}` `{{chairman}}` | اسما أمين السر ورئيس مجلس الإدارة |
| `{{decision_date}}` | تاريخ القرار |
| `{{org_name}}` `{{form_title}}` | اسم المأتم وعنوان الاستمارة |
| `{{today}}` `{{today_num}}` | تاريخ الطباعة |

كل حقل تضيفه بنفسك في تبويب **تحرير الاستمارة** يصبح متاحاً برمز يحمل مفتاحه.

**رقم الاجتماع:** يُطلب عند اعتماد كل طلب، ويمكن تركه فارغاً وإضافته لاحقاً من نافذة تعديل بيانات العضو.

**رقم العضوية:** رقم ثابت لا يتغير ولا يُعاد استخدامه بعد حذف عضو. الأعضاء المستوردون من الكشف الأصلي يحملون أرقامهم الأصلية نفسها.

**أسماء المعتمدين:** تُضبط من تبويب نموذج الطباعة. اتركها فارغة إذا كنت تفضّل التوقيع باليد.

**ملاحظات:**

- الرمز غير المعروف يبقى ظاهراً كما هو في الاستمارة المطبوعة، حتى تلاحظ الخطأ بدل أن يختفي الحقل بصمت.
- يعمل النظام حتى لو قسّم Word الرمز داخلياً بين أجزاء متعددة، وهو ما يحدث كثيراً ولا يظهر للمستخدم.
- تنسيق الملف كاملاً محفوظ: الخطوط والألوان والجداول والاتجاه من اليمين إلى اليسار.
- الحد الأقصى لحجم الملف عشرة ميغابايت، ويجب أن يكون بصيغة `.docx`.

### نظام الترشح والانتخابات

الترشح يتم من الأعضاء أنفسهم، وليس بإدخال يدوي من اللجنة. الإدخال اليدوي يبقى متاحاً كاستثناء.

**التسلسل:**

1. **المناصب أولاً.** تُضاف المناصب من تبويب إعداد التصويت. لا يمكن فتح باب الترشح قبلها.
2. **فتح باب الترشح.** يظهر عندها للأعضاء المسجّلين زر تقديم طلب الترشح في صفحاتهم.
3. **تقديم الطلب.** يختار العضو المنصب ويرفع صورته الشخصية. الاسم يؤخذ تلقائياً من بيانات عضويته، وتُبيّن له الصفحة ذلك.
4. **المراجعة.** لكل طلب خياران فقط: **قبول الترشح**، أو **إعادة للتعديل مع ملاحظة**. لا يوجد رفض ولا حذف؛ الطلب المعاد يستطيع صاحبه تعديله وإعادة إرساله، وتُمسح الملاحظة عند إعادة الإرسال.
5. **إغلاق باب الترشح.** شرط لازم لبدء التصويت.
6. **فتح التصويت.**

**شروط الترشح:** أن تكون العضوية نشطة، وأن يكون عمر المترشح 21 سنة فأكثر. يُحسب العمر من تاريخ الميلاد المسجّل، ويُبلَّغ العضو بالسبب إن لم ينطبق عليه الشرط.

**تغيير المنصب:**

| من | متى |
|---|---|
| العضو نفسه | ما دام باب الترشح مفتوحاً وطلبه لم يُقبل بعد |
| لجنة الانتخابات | في أي وقت، بما في ذلك بعد إغلاق باب الترشح |

**قواعد الفتح والإغلاق:**

- يمكن إعادة فتح باب الترشح ما دام التصويت لم يبدأ.
- لا يبدأ التصويت وباب الترشح مفتوح.
- يمكن إغلاق التصويت وإعادة فتحه في أي وقت.

**البيانات الظاهرة للناخبين:** تُضبط من تبويب المرشحين. الاسم يظهر دائماً، أما الصورة ووصف المنصب والترقيم فيمكن إخفاؤها. ما يُخفى لا يُرسل إلى المتصفح أصلاً.

#### الصور

| الشرط | القيمة |
|---|---|
| النسبة | مربعة 1:1 |
| الحد الأدنى | 512×512 بكسل |
| الحد الأعلى المخزّن | 1080×1080، والأكبر يُصغّر تلقائياً |
| حجم الملف | حتى 2 ميغابايت |
| الصيغ المقبولة | JPG، PNG، WEBP |

تُعالَج كل صورة على الخادم وتُحفظ نسختان: نسخة للعرض بحجم 1080 وأخرى مصغّرة 320 تُستخدم في صفحة التصويت. الصور تُحمّل عند الحاجة فقط، ومساحتها محجوزة مسبقاً فلا تهتز الصفحة أثناء التحميل، وتُخزَّن في المتصفح فلا تُطلب مرة أخرى.

#### صفحة التصويت

صفحة مستقلة لكل منصب، مع أزرار السابق والتالي والتنقل بينها بحرية، ثم صفحة مراجعة للاختيارات قبل الإرسال. بعد الإرسال لا يمكن الدخول إلى التصويت مرة أخرى. النتائج لا تظهر لأحد سوى لجنة الانتخابات.

### لوحة لجنة الانتخابات — `/elections`

| التبويب | الوظيفة |
|---|---|
| **إعداد التصويت** | اختيار نظام التصويت، عنوان الانتخابات ونص الإعلان، وإدارة المناصب. |
| **المرشحون** | إضافة المرشحين بالصورة والاسم والوصف، وتعديلهم أو حذفهم. |
| **النتائج** | عدد الأصوات والنسب والفائزون ونسبة المشاركة. |

**نظاما التصويت:**

- **حسب المناصب** — لكل منصب صفحة مستقلة، ويتنافس فيه مرشحوه وحدهم. لكل منصب عدد فائزين مستقل (مثال: رئيس واحد، وعضوان لمجلس الإدارة) وعدد اختيارات مستقل.
- **قائمة واحدة** — جميع المرشحين في صفحة واحدة، مع عدد فائزين وعدد اختيارات موحّد.

**حالات التصويت:** `إعداد` لا يظهر شيء للأعضاء ← `مفتوح` يظهر الإعلان ويُستقبل التصويت ← `مغلق` تتوقف الأصوات وتثبت النتائج.

النظام يمنع فتح التصويت إذا كان أحد المناصب بلا مرشحين، أو إذا كان عدد الاختيارات المسموحة أكبر من عدد المرشحين فيه.

**سرية النتائج:** عدد الأصوات والنسب لا تُرسل إطلاقاً إلى صفحات الأعضاء ولا إلى لوحة إدارة العضوية. البيانات التي تصل إلى متصفح العضو تحتوي على أسماء المرشحين وصورهم فقط.

### تجربة العضو

1. يدخل بـ **رقم الهاتف + تاريخ الميلاد + الرقم الشخصي** المسجّلة.
2. غير المسجّل يُوجَّه إلى الاستمارة، وبعد الإرسال تظهر رسالة `لقد تم ارسال طلب التقديم بنجاح`.
3. العضو المعتمد قبل إعلان الانتخابات يرى: `لا يوجد شيء حتى الآن، بانتظار الاعلان من لجنة الانتخابات`.
4. عند فتح التصويت يظهر زر الدخول إلى ورقة التصويت، مع مراجعة الاختيارات قبل الإرسال النهائي.
5. لا يمكن التصويت أكثر من مرة واحدة.

---

## النسخ الاحتياطي

كل شيء داخل مجلد البيانات (`/data` في الحاوية).

```bash
# نسخة احتياطية سليمة أثناء التشغيل
docker compose exec matam sh -c 'sqlite3 /data/matam.db ".backup /data/backup.db"' 2>/dev/null \
  || docker compose stop matam
docker run --rm -v matam-data:/data -v "$PWD:/backup" debian:bookworm-slim \
  tar czf /backup/matam-$(date +%F).tar.gz -C /data .
docker compose start matam
```

أبسط طريقة يومية: نزّل ملف Excel من لوحة إدارة العضوية واحتفظ به. لكنه لا يشمل الأصوات ولا بيانات المرشحين، لذلك انسخ المجلد كاملاً قبل يوم الانتخابات وبعده.

---

## ملاحظات فنية

**الأمان**

- كلمات المرور مخزّنة بخوارزمية PBKDF2-HMAC-SHA256 بـ 210,000 دورة وملح عشوائي لكل حساب.
- الجلسات رموز عشوائية 256 بت مخزّنة في قاعدة البيانات، صلاحيتها 12 ساعة، وملفات الارتباط `HttpOnly` و`SameSite=Strict`.
- حماية من تزوير الطلبات عبر التحقق من مصدر كل طلب يغيّر البيانات.
- تحديد معدل المحاولات على التسجيل وعلى تسجيل الدخول.
- صور المرشحين تُفحص بمحتواها الفعلي لا بامتداد اسمها، مع منع الخروج من مجلد الرفع.
- ترويسات `Content-Security-Policy` و`X-Content-Type-Options` و`Referrer-Policy` مفعّلة.

**منع التصويت المزدوج** مضمون بقيد فريد على مستوى قاعدة البيانات، لا بفحص برمجي فقط، فلا يفلت حتى مع طلبين متزامنين.

**ملف Excel** يُكتب بمكتبة داخلية مبنية على المكتبة القياسية فقط. كل خلية تُكتب كنص صريح بتنسيق `@`، وهو ما يضمن بقاء الأصفار الأولى بعد الفتح وإعادة الحفظ. الورقة من اليمين إلى اليسار مع تثبيت صف العناوين ومرشّح تلقائي.

**التبعيات:** حزمة خارجية واحدة فقط هي مشغّل SQLite (`github.com/mattn/go-sqlite3`). لا توجد أي مكتبة خارجية في الواجهة.

**بنية المشروع**

```
main.go                  نقطة التشغيل، تضمين الواجهة وكشف الأعضاء داخل الملف التنفيذي
internal/store/          قاعدة البيانات: الأعضاء، الطلبات، الانتخابات، الحسابات، سجل العمليات
internal/auth/           تشفير كلمات المرور ورموز الجلسات
internal/xlsxw/          كاتب ملفات Excel
internal/httpapi/        المسارات والتحقق والصلاحيات
web/pages/               صفحات HTML
web/static/              التنسيقات والسكربتات
seed/members.json        الأعضاء 378 المستوردون من الكشف الأصلي
```

---

تطوير: **محمود حميد بوراشد** — Instagram: [@m7burashid](https://instagram.com/m7burashid)
