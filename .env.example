# Copy to .env before the first `docker compose up -d`.
# These two accounts are created on first boot only.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-now-1234

ELECTIONS_USERNAME=elections
ELECTIONS_PASSWORD=change-this-too-5678
